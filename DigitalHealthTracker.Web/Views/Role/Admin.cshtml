@model List<DigitalHealthTracker.Web.ViewModels.TrainerPendingVm>

<h1>Admin Dashboard</h1>

<p>
    Welcome: @Context.Session.GetString("DisplayName")
    | Role: @Context.Session.GetString("Role")
</p>

<a href="/Account/Logout">
    <button type="button">Logout</button>
</a>


<hr />

@if (TempData["Msg"] is string msg)
{
    <p><b>@msg</b></p>
}

<h2>Pending Trainers</h2>

@if (Model.Count == 0)
{
    <p>No pending trainers.</p>
}
else
{
    <table border="1" cellpadding="6">
        <thead>
            <tr>
                <th>Id</th>
                <th>Full Name</th>
                <th>Phone</th>
                <th>Email</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var t in Model)
            {
                <tr>
                    <td>@t.Id</td>

                    <!-- ✅ FIX: FullName yerine Display kullan -->
                    <td>@t.Display</td>

                    <td>@t.Phone</td>
                    <td>@t.Email</td>
                    <td>
                        <form asp-controller="Role" asp-action="ApproveTrainer" method="post">
                            @Html.AntiForgeryToken()
                            <input type="hidden" name="id" value="@t.Id" />
                            <button type="submit">Approve</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
